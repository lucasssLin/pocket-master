<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.product.mapper.CategoryBrandMapper">


    <select id="selectCategoryBrandList" resultType="com.ruoyi.product.domain.CategoryBrand">
        select
        cb.*,
        c.name as categoryName,
        b.name as brandName,
        b.logo
        from `pocket-product`.category_brand cb
        left join `pocket-product`.category c on c.id = cb.category_id and c.del_flag = 0
        left join `pocket-product`.brand b on b.id = cb.brand_id and b.del_flag = 0
        <where>
            cb.del_flag = 0
            <if test="brandId!=null and brandId!=''">
                and cb.brand_id = #{brandId}
            </if>
            <if test="categoryId!=null and categoryId!=''">
                and cb.category_id = #{categoryId}
            </if>
        </where>
    </select>


    <select id="selectBrandListByCategoryId" resultType="com.ruoyi.product.api.domain.Brand">
        select
        b.id, b.name, b.logo
        from `pocket-product`.category_brand cb
        left join `pocket-product`.brand b on cb.brand_id = b.id and b.del_flag = 0
        where cb.del_flag = 0 and cb.category_id = #{categoryId}
    </select>


</mapper>
